# Example .env for Multi-Agent Medical Assistant
# Fill in your own API keys and endpoints before running the app

# LLM Configuration (Azure Open AI or OpenRouter)
deployment_name = test
model_name = openai/gpt-4o-mini
azure_endpoint = 
openai_api_version = 
# OpenRouter key if u use openrouter or openai if u use azure
openai_api_key = 

# OpenRouter LLM (instead of Azure) 
OPENAI_API_BASE=https://openrouter.ai/api/v1

# Embedding Model Configuration (Azure Open AI - text-embedding-ada-002 used in development)
# If using any other embedding model, appropriate code modification is required
# Example: EMBEDDING_MODEL_NAME=text-embedding-ada-002
# EMBEDDING_API_KEY=

# ElevenLabs API Key for TTS (if used)
# ELEVEN_LABS_API_KEY=

# FastAPI backend URL
# API_URL=http://localhost:8001

# Other configuration as needed
# Add more variables as required by your deployment
